### Example environment variables for portofolio-website
# Copy this file to `.env.local` for local development and fill in real values.

# Public URL of the site (used by canonical links, Open Graph, etc.)
NEXT_PUBLIC_SITE_URL=http://qportofolio.vercel.app

# Public API endpoint used by the frontend (if any)
NEXT_PUBLIC_API_URL=https://api.qportofolio.vercel.app

# Primary color used in the site (exposed to client)
NEXT_PUBLIC_PRIMARY_COLOR=#00ADB5

# Server-side secrets (do NOT commit real secrets here). Examples only:
# NEXTAUTH_SECRET=your-secret
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# Add other variables used by your app below.
